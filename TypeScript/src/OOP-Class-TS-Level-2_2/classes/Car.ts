class Car {
    _brand: string;
    _constructionYear: number;
    _maxSpeed: number;

    constructor(brand: string, constructionYear: number, maxSpeed: number) {
        this._brand = brand;
        this._constructionYear = constructionYear;
        this._maxSpeed = maxSpeed;
    }
}
export default Car;
